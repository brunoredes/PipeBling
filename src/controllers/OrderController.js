// import axios from 'axios';
// import api from '../service/api';

export default {
  async store(request, response) {
    // method to create a new order into bling '/produto'

    return response.status(201).json({ message: 'created' });
  },
};
